# 🚀 AWS后端部署 - 快速开始

## 🎯 您要做的事情

将Spring Boot后端应用部署到AWS，包括：
- ✅ PostgreSQL数据库 (RDS)
- ✅ Spring Boot应用 (EC2)
- ✅ 自动化部署脚本
- ✅ CI/CD配置

## ⚡ 最快速的部署方式

### 步骤1: 进入部署目录

\`\`\`bash
cd deployment
\`\`\`

### 步骤2: 运行快速向导

\`\`\`bash
./quick-start.sh
\`\`\`

### 步骤3: 选择部署方式

向导会提示您选择：

**选项1️⃣: AWS CLI 自动创建资源** (推荐首次部署)
- 自动创建RDS + EC2 + 安全组
- 预计时间: 10-15分钟

**选项2️⃣: 手动部署** (已有AWS资源)
- 需要提供EC2 IP和SSH密钥
- 预计时间: 5-10分钟

**选项3️⃣: 本地Docker测试**
- 在本地测试完整环境
- 预计时间: 2-3分钟

## 📚 详细文档

所有文档都在 \`deployment/\` 目录下：

| 文件 | 说明 |
|------|------|
| \`DEPLOYMENT_SUMMARY.md\` | **完整部署方案总览** ⭐ |
| \`aws-setup-guide.md\` | 详细步骤指南 (300+行) |
| \`README.md\` | 工具包使用说明 |

## 🔧 自动化脚本

| 脚本 | 用途 |
|------|------|
| \`quick-start.sh\` | 交互式部署向导 ⭐ |
| \`aws-cli-setup.sh\` | 自动创建AWS资源 |
| \`deploy.sh\` | 应用部署/更新 |

## 🐳 Docker本地测试

\`\`\`bash
cd deployment
docker-compose up -d

# 测试API
curl http://localhost:8080/api/health

# 停止
docker-compose down
\`\`\`

## 💡 推荐流程

### 首次部署

1. **本地测试** (可选)
   \`\`\`bash
   cd deployment && docker-compose up -d
   \`\`\`

2. **AWS部署**
   \`\`\`bash
   cd deployment && ./quick-start.sh
   # 选择"1) AWS CLI 自动创建资源"
   \`\`\`

3. **验证部署**
   \`\`\`bash
   curl https://api.your-domain.com/api/health
   \`\`\`

### 后续更新

\`\`\`bash
cd deployment

# 设置环境变量（一次性）
export EC2_HOST=ec2-user@your-ec2-ip
export SSH_KEY=./your-key.pem

# 运行部署
./deploy.sh
\`\`\`

## ⚠️ 前提条件

### 必需

- ✅ AWS账号
- ✅ AWS CLI已配置 (\`aws configure\`)
- ✅ Java 17 (本地构建需要)
- ✅ Maven (本地构建需要)

### 可选

- Docker (本地测试需要)
- GitHub账号 (CI/CD需要)

## 💰 成本估算

### 免费套餐 (12个月)
- EC2 t2.micro: **免费**
- RDS db.t3.micro: **免费**
- **总计: $0/月**

### 小规模生产
- EC2 t3.small: $15/月
- RDS db.t3.small: $30/月
- **总计: ~$50/月**

## 🆘 需要帮助？

1. **查看完整文档**
   \`\`\`bash
   cat deployment/DEPLOYMENT_SUMMARY.md
   \`\`\`

2. **查看故障排查**
   \`\`\`bash
   cat deployment/aws-setup-guide.md | grep "故障排查"
   \`\`\`

3. **查看日志**
   \`\`\`bash
   ssh -i your-key.pem ec2-user@your-ec2-ip
   sudo journalctl -u kyle-portfolio -f
   \`\`\`

---

## 🎉 开始部署！

\`\`\`bash
cd deployment
./quick-start.sh
\`\`\`

**就这么简单！**
